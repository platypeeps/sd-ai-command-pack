# Review Learnings

## Curated Lessons

- When editing Trellis journal entries, target the intended session heading
  explicitly. Avoid broad replacement of fallback text such as "Detailed change
  bullets were not supplied" or "Validation was not recorded"; older sessions
  often contain the same fallback text and must remain historically accurate.
  The distributed review preflight now enforces this by rejecting changes to a
  review-base session older than the current session.

<!-- sd-review-learnings:start -->
## SD Review Learnings

_Last updated: 2026-07-21_

### Local Pattern Findings
- No local review-cycle findings detected in the scanned diff.

### Recent Copilot Review Signals
- **historical** PR #206 `templates/docs/SD_AI_COMMAND_PACK.md`: The text says “dirty tree”, but full-check’s Prism scope decision is based on `git diff` / `git diff --cached`, which ignore untracked files. Consider rephrasing to make it clear this is about tracked... (https://github.com/platypeeps/sd-ai-command-pack/pull/206)
- **historical** PR #206 `templates/docs/SD_AI_COMMAND_PACK.md`: Same “dirty trees” wording here could be interpreted as including untracked files, but Prism scope selection is based on tracked diffs. Clarifying the wording would better match the implementation and the PRD’s note... (https://github.com/platypeeps/sd-ai-command-pack/pull/206)
- **historical** PR #206 `README.md`: “Dirty trees” is ambiguous (often read as `git status` including untracked files), but the Prism lane’s local-first behavior is based on tracked staged/unstaged diffs (`git diff` / `git diff --cached`). Consider... (https://github.com/platypeeps/sd-ai-command-pack/pull/206)
- **historical** PR #206 `templates/.agents/skills/sd-full-check/SKILL.md`: “Dirty trees” here could be interpreted as including untracked files, but the Prism selection logic is driven by tracked staged/unstaged diffs. Rephrasing would make the contract more precise and consistent with the... (https://github.com/platypeeps/sd-ai-command-pack/pull/206)
- **historical** PR #206 `CHANGELOG.md`: The changelog entry uses “dirty trees”, which can be read as including untracked files, but the Prism lane’s local-first behavior is based on tracked staged/unstaged diffs. Consider tightening the wording to match... (https://github.com/platypeeps/sd-ai-command-pack/pull/206)
- **historical** PR #205 `templates/scripts/sd-ai-command-pack-full-check.sh`: The new fail-closed branch for an unverifiable ignored state (git check-ignore returning a non-0/non-1 status) is not covered by the full-check KB lane tests. Since this is a distinct safety behavior (refuse... (https://github.com/platypeeps/sd-ai-command-pack/pull/205)
- **historical** PR #205 `tests/test_full_check.py`: This test builds a PATH value via os.environ['PATH'], which can raise KeyError in minimal environments where PATH is unset (making the test fail before exercising the intended behavior). Use os.environ.get('PATH',... (https://github.com/platypeeps/sd-ai-command-pack/pull/205)
- **historical** PR #205 `templates/scripts/sd-ai-command-pack-full-check.sh`: The auto-repair path assumes `git` is available; if `git` is missing (or not on PATH), this will fall into the “ignored state could not be verified” branch and still instruct users to run `git check-ignore ...`,... (https://github.com/platypeeps/sd-ai-command-pack/pull/205)
- **historical** PR #205 `tests/test_full_check.py`: This test assumes removing `.obsidian-kb/` from the repo’s `.gitignore` will make `.obsidian-kb` unignored. On systems with a global excludes file (e.g., `~/.config/git/ignore`) that ignores `.obsidian-kb`, `git... (https://github.com/platypeeps/sd-ai-command-pack/pull/205)
- **historical** PR #205 `.trellis/tasks/archive/2026-07/07-21-self-heal-stale-obsidian-kb/prd.md`: The PRD’s cited fleet candidate payload digest does not match the digest recorded in docs/fleet/candidate-validation.json for pack 0.25.4. This makes the task evidence internally inconsistent and can confuse future... (https://github.com/platypeeps/sd-ai-command-pack/pull/205)
- **historical** PR #196 `.trellis/workspace/sdelmas/journal-4.md`: The session journal says validation was not recorded, but this PR includes updated fleet candidate evidence and describes a completed validation run. For traceability, record the validations performed (or adjust the... (https://github.com/platypeeps/sd-ai-command-pack/pull/196)
- **historical** PR #193 `templates/scripts/sd-ai-command-pack-work-loop.py`: `unchanged_historical_shipped_evidence` can become true when both `candidate_branch` and `baseBranch` are `None` (`None == None`). That would skip the verifiable-tip and shipped-branch ancestry checks even though... (https://github.com/platypeeps/sd-ai-command-pack/pull/193)
- **historical** PR #193 `.trellis/workspace/sdelmas/journal-4.md`: The session journal says validation “was not recorded”, but this PR’s metadata lists concrete validation steps (tests/lint/full-check/fleet). This entry will be misleading for future debugging/audits. Either record... (https://github.com/platypeeps/sd-ai-command-pack/pull/193)
- **historical** PR #192 `templates/scripts/sd-ai-command-pack-status.py`: `_port_number` is assigned but never used. Since this line exists only to force `SplitResult.port` evaluation (and therefore trigger `ValueError` for invalid ports inside the try/except), using the conventional... (https://github.com/platypeeps/sd-ai-command-pack/pull/192)
- **historical** PR #197 `.trellis/tasks/archive/2026-07/07-20-fix-terminal-reconciliation-status-diagnostic/task.json`: `base_branch` is documented/used as the PR target branch (see `.trellis/scripts/task.py` / `.trellis/scripts/common/task_store.py`), but this task record sets it to the feature branch name. That can misdirect... (https://github.com/platypeeps/sd-ai-command-pack/pull/197)
- **historical** PR #195 `templates/scripts/sd-ai-command-pack-work-loop.py`: `acquire_terminal_lock()` raises for an active terminal reconciliation lock without any wait/retry guidance, but this PR (and the updated adapter guidelines) establish that active owners should tell operators to... (https://github.com/platypeeps/sd-ai-command-pack/pull/195)
- **historical** PR #195 `templates/scripts/sd-ai-command-pack-work-loop.py`: `acquire_terminal_lock()` wraps malformed/unreadable lock JSON, but `lock_is_stale()` can also raise `WorkLoopError` (e.g., malformed `heartbeatAt`) and currently escapes without the intended terminal-lock... (https://github.com/platypeeps/sd-ai-command-pack/pull/195)
- **historical** PR #199 `scripts/sd_ai_command_pack_fleet_lib.py`: This error message hard-codes "fleet manifest" and ignores the caller-provided label (which sometimes includes the manifest path), reducing context when diagnosing which manifest failed parsing. (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `scripts/sd_ai_command_pack_fleet_lib.py`: Consumer-scoped config errors use a hard-coded prefix ("fleet manifest consumer …") instead of the provided label. Using the label here would automatically include the richer context (e.g., "fleet manifest <path>")... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `templates/scripts/sd_ai_command_pack_fleet_lib.py`: The sequential cohort maxConcurrency validation is type-loose: values like true or 1.0 will currently be accepted because the check only compares against != 1 (and bool/float compare equal to 1). This weakens the... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `scripts/sd-ai-command-pack-fleet-wave-plan.py`: _load_json_object() does an lstat() check but then re-opens the path via read_text(). That leaves a TOCTOU window where the path can be swapped after the lstat (including to a symlink or a larger file), undermining... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `tests/test_fleet_candidate.py`: Many entries in `invalid_manifests` still use `schemaVersion: 3`. Now that `FLEET_SCHEMA_VERSION` is 4, those cases will fail at the schema gate (and/or missing `rolloutPolicy`) before exercising the consumer-field... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `templates/scripts/sd_ai_command_pack_fleet_lib.py`: parse_fleet_rollout_policy() currently normalizes cohort consumer names via strip().casefold(), which means rolloutPolicy can silently accept leading/trailing whitespace and casing mismatches (e.g., " CANARY-A ")... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `scripts/sd-ai-command-pack-fleet-wave-plan.py`: The new wave planner treats `pr-open` as a canary failure state because `CANARY_SUCCESS_STATES` excludes it. This conflicts with the `sd-fleet-refresh` skill’s `no-merge` contract ("Stop every consumer at PR-open"),... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `tests/install_test_support.py`: The `fleet_manifest()` test helper sorts cohort consumer order using `consumer["name"]` with case-sensitive ordering, but production ordering uses `consumer.name.casefold()` (see `parse_fleet_consumers()` sorting).... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `templates/scripts/sd_ai_command_pack_fleet_lib.py`: load_fleet_rollout_policy() currently parses the rollout policy twice: parse_fleet_consumers() already calls parse_fleet_rollout_policy(manifest, ordered, label) for schema-4 validation, and then... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #199 `scripts/sd-ai-command-pack-fleet-wave-plan.py`: In main(), consumers are parsed via fleet_lib.parse_fleet_consumers(manifest), which already validates the schema-4 rolloutPolicy (it calls parse_fleet_rollout_policy internally). Immediately calling... (https://github.com/platypeeps/sd-ai-command-pack/pull/199)
- **historical** PR #191 `templates/scripts/sd-ai-command-pack-review-full-check.sh`: The package.json inspection treats any OSError (e.g., permission denied / unreadable file) as “invalid configuration” (exit 10), which silently falls back to the pack full-check. That contradicts the intended... (https://github.com/platypeeps/sd-ai-command-pack/pull/191)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: When `branch_changed` is true and the remembered feature branch no longer exists locally, this code falls back to `candidate_shipped` as the `evidence_tip`. That makes the `lastShippedSha` check effectively... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: `reconcile_state()` currently treats any active recovery checkpoint (including `paused`) as requiring complete current-state evidence even when the caller supplies *no* observations (no phase and no current fields).... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: In `stop --status paused`, when `prior_phase` is already "checkpoint" (legacy schema-v1 runs), the new code overwrites `checkpoint.target` with the literal string "checkpoint", discarding the prior human/phase... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: `_has_complete_recovery_evidence()` returns false when there are zero non-null recorded current-state fields. At a task boundary (e.g. inventory) that makes `reconcile_state()` treat even a "complete" recovery... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: Same issue as above for `complete_recovery_evidence`: if the ledger has no non-null recorded current-state fields (common at boundaries), the current computation can never report complete evidence, so a... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: `checkpoint` command will raise WorkLoopError on schema-v1 ledgers where `phase == "checkpoint"` but the checkpoint has no `resumePhase` and a human-only `target` (because `checkpoint_resume_phase()` requires... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: `stop --status paused` calls `checkpoint_resume_phase()` when the current ledger is legacy `phase == "checkpoint"`. For schema-v1 ledgers with a human-only checkpoint target and no `resumePhase`, this raises a... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #190 `templates/scripts/sd-ai-command-pack-work-loop.py`: `LIFECYCLE_PHASES = PHASES[:8]` hard-codes the lifecycle/control boundary via a magic index. If `PHASES` ever gains/reorders lifecycle steps, this will silently misclassify phases (and could break `--resume-phase`... (https://github.com/platypeeps/sd-ai-command-pack/pull/190)
- **historical** PR #189 `.trellis/tasks/07-20-terminal-work-loop-reconciliation/task.json`: `base_branch` is set to a task/work branch name (`codex/...`). In this repo, planning tasks consistently use `base_branch: "main"` (e.g. `.trellis/tasks/07-20-fleet-post-canary-waves/task.json:16`). Using a... (https://github.com/platypeeps/sd-ai-command-pack/pull/189)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: ABSOLUTE_PATH_RE only matches paths at start-of-string or after whitespace. That means an absolute path in parentheses/quotes (e.g. "( /Users/..." or "(/Users/...)" after normalization) can slip through... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: The JSON and stderr error outputs use str(exc) directly. Several FleetTimingError messages wrap raw OSError text (e.g. permission denied / no such file) which typically includes absolute filenames, potentially... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: In operation_lock(), a transiently invalid lock file (e.g., another process has created the lock but hasn’t finished writing valid JSON yet, or it crashed after create) currently causes an immediate FleetTimingError... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: The aggregate reviewer/CI overlap is currently derived by summing per-consumer overlaps (computed earlier in build_summary). If reviewer/CI waits overlap across consumers (e.g., a later consumer starts before a... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: Privacy contract/docs say durable state and normal output must not contain a remote URL. As written, safe_reason() rejects secrets and absolute/home-relative filesystem paths, but it still allows inputs like... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: docs/FLEET_ROLLOUT.md states the human summary should report interval-union active wall time, but render_human() only prints critical path, summed stage elapsed, overlap, and retries. That makes the CLI output... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: Per docs/FLEET_ROLLOUT.md, the human summary is also expected to surface per-consumer interval-union active wall time. The per-consumer lines currently include only critical-path and retry count, so operators can’t... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #188 `scripts/sd-ai-command-pack-fleet-timing.py`: _validate_lock() treats "schemaVersion" as a plain equality check. Because `bool` is a subclass of `int` in Python, a lock file containing `"schemaVersion": true` would currently be accepted as schema version 1,... (https://github.com/platypeeps/sd-ai-command-pack/pull/188)
- **historical** PR #184 `templates/.agents/skills/sd-fleet-refresh/SKILL.md`: The skill suggests running `sd-ai-command-pack-fleet-candidate-check.py` without the `scripts/` path or the toolchain wrapper, but the canonical procedure in docs/FLEET_ROLLOUT.md runs it via `bash... (https://github.com/platypeeps/sd-ai-command-pack/pull/184)
- **historical** PR #184 `.trellis/tasks/07-20-fleet-rollout-timing-telemetry/implement.jsonl`: This new Trellis task jsonl file still contains the scaffold `_example` placeholder line (it even says to delete it). That contradicts the PR checklist claim that task notes contain no placeholders, and it also adds... (https://github.com/platypeeps/sd-ai-command-pack/pull/184)
- **historical** PR #181 `CHANGELOG.md`: The changelog entry describes only values that become empty after normalization, but the implementation also rejects non-string transition values (e.g., int) before normalization. This makes the release note... (https://github.com/platypeeps/sd-ai-command-pack/pull/181)
- **historical** PR #179 `tests/test_work_loop.py`: This assertion depends on argparse including the unknown option's value ("HEAD") in the exact error text, which can vary across Python/argparse versions. Checking only for the unknown flag keeps the test validating... (https://github.com/platypeeps/sd-ai-command-pack/pull/179)
- **historical** PR #179 `tests/test_work_loop.py`: This test reaches into argparse private attributes ("_subparsers" / "_group_actions") to get the transition subparser help, which is brittle. Prefer asserting help output via the public CLI/parsing behavior (e.g.,... (https://github.com/platypeeps/sd-ai-command-pack/pull/179)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: `transition_state` rejects stable-field updates by comparing the raw incoming value against the stored compacted value (`remembered != value`). Since the assignment later uses `compact_text(value)`, a no-op update... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: `validated_evidence` verifies that `head` resolves to a commit (`resolved_head`), but it stores the *original user input* in `candidate["head"]`. That allows symbolic values like `HEAD` (or other commit-ish strings)... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: Similar to `head`, `validated_evidence` verifies that `lastShippedSha` resolves to a commit (`resolved_shipped`), but it keeps the original input string in the persisted ledger. Persisting symbolic commit-ish values... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: `validated_evidence` accepts `--branch` updates without verifying that the branch actually exists locally when no `head` is provided (e.g., `evidence --branch does-not-exist`). That can persist an invalid branch... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: When a recorded branch no longer exists locally, `_branch_commit()` returns `None` and the code raises `HEAD evidence does not match the recorded branch`, which is misleading (the problem is the missing branch ref,... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #172 `templates/scripts/sd-ai-command-pack-work-loop.py`: `reconcile_state` compares raw observation strings against compacted/normalized ledger values when building `mismatches`. This can mark formatting-only differences (e.g., trailing whitespace in `--task`, `--branch`,... (https://github.com/platypeeps/sd-ai-command-pack/pull/172)
- **historical** PR #170 `templates/scripts/sd-ai-command-pack-status.py`: The anomaly preview for completed active-root tasks uses only task["id"]. When a task.json "id" differs from its directory name (common for date-prefixed task dirs), the sd-status output becomes ambiguous and the... (https://github.com/platypeeps/sd-ai-command-pack/pull/170)
- **historical** PR #170 `templates/scripts/sd-ai-command-pack-review-preflight.mjs`: `checkCompletedTrellisTaskLocation()` is documented/tests as ignoring symlinked task entries, but the directory scan currently relies on `Dirent.isDirectory()` only. Since `isRegularFile()` lstat-checks only... (https://github.com/platypeeps/sd-ai-command-pack/pull/170)
- **historical** PR #169 `.trellis/tasks/07-19-status-snapshot-contract-validation/task.json`: The PR description says this change set completes `07-19-status-snapshot-contract-validation`, but the task metadata still has `"status": "in_progress"` and `"completedAt": null`. If this PR is the completion... (https://github.com/platypeeps/sd-ai-command-pack/pull/169)
- **historical** PR #167 `.trellis/tasks/archive/2026-07/07-19-review-learnings-0-21-4-rollout/prd.md`: The PRD references the follow-up task as `07-19-status-snapshot-contract-validation`, but other Trellis PRDs in this repo refer to tasks by their `id`/`name` (e.g., `status-snapshot-contract-validation`). Using the... (https://github.com/platypeeps/sd-ai-command-pack/pull/167)
- **historical** PR #167 `docs/review-learnings.md`: The managed `sd-review-learnings` block no longer matches the renderer contract in `scripts/sd-ai-command-pack-review-learnings.py` (`PullRequestComment.markdown_item()` returns `PR #N `{path}`: ...` at line ~172).... (https://github.com/platypeeps/sd-ai-command-pack/pull/167)
- **historical** PR #167 `.trellis/tasks/archive/2026-07/07-19-review-learnings-0-21-4-rollout/task.json`: The archived task notes reference the follow-up using the dated directory name (`07-19-review-learning-remote-path-rendering`). Elsewhere in this PR (e.g., the archived PRD) the follow-up is referenced via its... (https://github.com/platypeeps/sd-ai-command-pack/pull/167)
- **historical** PR #167 `.trellis/tasks/archive/2026-07/07-19-review-learnings-0-21-4-rollout/prd.md`: The archived PRD’s Results section says the *only* remaining hardening idea is tracked by `status-snapshot-contract-validation`, but this PR also creates/tracks the follow-up task... (https://github.com/platypeeps/sd-ai-command-pack/pull/167)
- **historical** PR #154 `templates/scripts/sd-ai-command-pack-status.py`: The local variable name `git_root` is misleading here: it’s used as the working directory passed to `git -C`, while the actual Git root is obtained from `rev-parse --show-toplevel` later. Renaming it avoids... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 `templates/scripts/sd-ai-command-pack-status.py`: `resolve_repo()` passes the same (potentially relative) path as both `git -C` and `cwd`. When `--repo` is a relative path from outside the repo (e.g., `--repo some-repo` or `--repo some-repo/README.md`), `cwd`... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 `tests/test_status.py`: This regression test only exercises absolute `Path` inputs. Given `resolve_repo()` is fed directly from `argparse(type=Path)`, users can pass relative paths like `--repo some-repo/README.md` from the parent... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 `.trellis/tasks/07-18-fleet-refresh-0-19-3/task.json`: Task identifier `fleet-refresh-0-19-3` (and the directory name) conflicts with the task title/description targeting 0.19.4. This makes it harder to search/track rollouts by version and can be confusing when multiple... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)

### Suggested Preventive Actions
- Move repeated mechanical findings into local checks where possible.
- Keep Copilot instructions focused on current, non-outdated unresolved findings.
- Treat generated or copied payloads as source/sync-contract review surfaces, not style-review surfaces.
<!-- sd-review-learnings:end -->
