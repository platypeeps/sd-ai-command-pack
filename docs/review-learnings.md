# Review Learnings

## Curated Lessons

- When editing Trellis journal entries, target the intended session heading
  explicitly. Avoid broad replacement of fallback text such as "Detailed change
  bullets were not supplied" or "Validation was not recorded"; older sessions
  often contain the same fallback text and must remain historically accurate.
  The distributed review preflight now enforces this by rejecting changes to a
  review-base session older than the current session.

<!-- sd-review-learnings:start -->
## SD Review Learnings

_Last updated: 2026-07-19_

### Local Pattern Findings
- No local review-cycle findings detected in the scanned diff.

### Recent Copilot Review Signals
- **historical** PR #154 review path templates/scripts/sd-ai-command-pack-status.py: The local variable name `git_root` is misleading here: it’s used as the working directory passed to `git -C`, while the actual Git root is obtained from `rev-parse --show-toplevel` later. Renaming it avoids... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 review path templates/scripts/sd-ai-command-pack-status.py: `resolve_repo()` passes the same (potentially relative) path as both `git -C` and `cwd`. When `--repo` is a relative path from outside the repo (e.g., `--repo some-repo` or `--repo some-repo/README.md`), `cwd`... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 review path tests/test_status.py: This regression test only exercises absolute `Path` inputs. Given `resolve_repo()` is fed directly from `argparse(type=Path)`, users can pass relative paths like `--repo some-repo/README.md` from the parent... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #154 review path .trellis/tasks/07-18-fleet-refresh-0-19-3/task.json: Task identifier `fleet-refresh-0-19-3` (and the directory name) conflicts with the task title/description targeting 0.19.4. This makes it harder to search/track rollouts by version and can be confusing when multiple... (https://github.com/platypeeps/sd-ai-command-pack/pull/154)
- **historical** PR #164 review path tests/test_work_loop.py: This test patches `module.os.chmod`, but `atomic_write_json()` tightens permissions via `path.chmod(0o600)` (a `Path.chmod` call). Patching `module.os.chmod` won’t simulate an unsupported chmod for the final... (https://github.com/platypeeps/sd-ai-command-pack/pull/164)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `canonical_remote()` treats Windows drive paths like `C:/Users/...` as SCP-style remotes (because of the `host:path` regex) and rewrites them to `ssh://C/...`, which breaks repository identity hashing for local-path... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: The `rank` subcommand defines a `--json` flag, but the implementation always prints JSON via `_print(..., as_json=True)`. Without `--json`, `_print` would also emit no output for the `rank` payload shape, so the... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `validate_state()` considers any dict with `focus.mode` valid, but later code (e.g. `status_snapshot`) assumes `focus.original` exists and is a list. A malformed-but-schemaVersion-correct ledger can therefore raise... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-status.py: `collect_work_loop()` doesn’t catch `AttributeError`/`SyntaxError` from the helper load path (e.g., missing `status_snapshot` or a corrupted helper file). That would crash `sd-status` instead of returning a... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path scripts/sd-ai-command-pack-work-loop.py: `status_snapshot()` only catches `WorkLoopError`, but it indexes into the validated state with required keys like `state["mode"]`, `state["selector"]`, and `state["until"]`. If the on-disk ledger is missing or has... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-status.py: `collect_work_loop()` does not catch `KeyError`/`TypeError` from the helper call path. The work-loop helper currently indexes required state keys (e.g. `state["mode"]`) and can raise these if the user-local ledger... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path scripts/sd-ai-command-pack-work-loop.py: When resuming an existing active/paused run, `start` ignores the caller-provided `--mode/--selector/--until` and any focus arguments (they’re parsed/normalized but never applied). This can silently resume the wrong... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `validate_state()` enforces MAX_LEDGER_BYTES using a compact JSON encoding, but `atomic_write_json()` writes an indented payload. A state can therefore pass validation yet fail to persist with "refusing to write... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: If `lock.json` exists but is unreadable/corrupt, `acquire_lock()` fails hard because `read_json()` raises, and `--recover-stale-lock` cannot help (since the failure happens before staleness is evaluated). Consider... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `acquire_lock()` cannot recover from a lock file that is *readable JSON* but malformed (e.g., missing/invalid `heartbeatAt`). In that case `lock_is_stale()` returns False, so even with `--recover-stale-lock` the... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `reconcile_state()` leaves `contextHealth.level` stuck at `amber`: when a signal (e.g. compaction/continuation-summary) sets amber, a later successful reconcile call with no signal and no contradictions will not... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #162 review path templates/scripts/sd-ai-command-pack-work-loop.py: `canonical_remote()` preserves the full URL netloc (including any embedded credentials like `https://<token>@github.com/org/repo.git` or `https://user:pass@...`). Because `repository_identity()` stores this... (https://github.com/platypeeps/sd-ai-command-pack/pull/162)
- **historical** PR #161 review path templates/docs/SD_AI_COMMAND_PACK.md: The explicit equivalent invocation is missing the `sd-status` command name, which makes the docs ambiguous (it reads like a bare `--repo` shell option). Clarify by showing the full explicit form (`sd-status --repo ...`). (https://github.com/platypeeps/sd-ai-command-pack/pull/161)
- **historical** PR #161 review path templates/.agents/skills/sd-status/SKILL.md: The workflow invocation snippet still shows both a positional repo-path and `--repo PATH` as independently optional, but the collector now rejects combining them. Tweaking the bracketed alternatives will keep the... (https://github.com/platypeeps/sd-ai-command-pack/pull/161)
- **historical** PR #160 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: The file-read `.split(...)` heuristics won’t match common direct boundary cases where the read call’s argument list contains nested parentheses (e.g. `readFileSync(resolve(root, name), 'utf8').split('\n')`). Both... (https://github.com/platypeeps/sd-ai-command-pack/pull/160)
- **historical** PR #160 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: The direct file-read split heuristic won’t match common namespaced Node patterns like `fs.readFileSync(...).split(...)` because the regex only matches unqualified `readFileSync(` / `readFile(` calls. This can miss... (https://github.com/platypeeps/sd-ai-command-pack/pull/160)
- **historical** PR #160 review path tests/test_review_preflight.py: Add a regression for the common `fs.readFileSync(...).split(...)` form so the narrowed structured-input heuristic still flags direct file-content splits even when the read is namespaced. (https://github.com/platypeeps/sd-ai-command-pack/pull/160)
- **historical** PR #160 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: The `read_text(...).split(...)` boundary heuristic is currently broad enough to match any `read_text(` function call (not necessarily a `Path(...).read_text(...)` file read), which can reintroduce noisy... (https://github.com/platypeeps/sd-ai-command-pack/pull/160)
- **historical** PR #157 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: When oversized untracked code is skipped from the boundary-risk content scan, the subsequent PASS message (“no boundary-risk trigger was added”) can be misleading because the scan was incomplete (a trigger could... (https://github.com/platypeeps/sd-ai-command-pack/pull/157)
- **historical** PR #157 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: `boundedUntrackedFileText()` opens the untracked path before doing the size check, and returns `null` on any open/fstat error. That means an oversized-but-unopenable untracked file (permissions, transient deletion,... (https://github.com/platypeeps/sd-ai-command-pack/pull/157)
- **historical** PR #157 review path tests/test_review_preflight.py: This regression covers the *oversized* skip path (via `untrackedFileReadLimitBytes: 8`), but it doesn’t exercise the new descriptor open/read loop or the “open/read failures are advisory” behavior (because the... (https://github.com/platypeeps/sd-ai-command-pack/pull/157)
- **historical** PR #155 review path tests/test_review_preflight.py: The assertion failure message uses stdout, but this failure path is expected to write diagnostics to stderr (you assert on stderr on the next line). If this assert fails, the current message may be empty/unhelpful;... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #155 review path .github/workflows/tests.yml: The `gh api | jq` pipeline is inside a command substitution without `pipefail`, so an API/auth failure can be masked (jq may still exit 0 on empty input) and then fail later with a confusing `-gt` integer error.... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #155 review path .github/workflows/tests.yml: The workflow-level `pull-requests: read` permission applies to all jobs/events, even though only the `main-push-scope` job needs it for `gh api .../commits/.../pulls`. To reduce token scope, consider moving... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #155 review path .github/workflows/tests.yml: The step unconditionally calls the GitHub commit-to-PR API before running the scope guard. If the API call fails (rate limit/outage/auth), this will fail the job even for traditional merge commits (which the guard... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #155 review path templates/scripts/sd-ai-command-pack-status.py: `resolve_repo()` now passes `cwd=Path.cwd()` into `run_command()`. That introduces an avoidable dependency on the caller’s current working directory even when `git_path` is an absolute directory (and can break if... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #155 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: `parseTrellisTaskArtifactPath()` now accepts .trellis/tasks/archive/task.json because `match[1]` can be exactly `archive` via the `[^/]+` alternative. That path is not a valid Trellis task directory (archived... (https://github.com/platypeeps/sd-ai-command-pack/pull/155)
- **historical** PR #150 review path templates/scripts/sd-ai-command-pack-review-learnings.py: The GraphQL PR inventory query omits the `states` filter; in GitHub's GraphQL API `repository.pullRequests` defaults to OPEN-only, which would silently drop merged/closed PRs compared to the prior `gh pr list... (https://github.com/platypeeps/sd-ai-command-pack/pull/150)
- **historical** PR #149 review path templates/scripts/sd-ai-command-pack-status.py: `collect_git()` can return an empty dict when `git status` is unavailable, but `collect_local()` continues and later `render_local()` assumes keys like `git["workingTree"]`, which will raise `KeyError` (traceback)... (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path templates/scripts/sd-ai-command-pack-status.py: `fleet_api()` conditionally prepends `scripts_dir` to `sys.path` but never removes it. When this module is imported (as in unit tests), this mutates interpreter-global import state and can create order-dependent... (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path .trellis/tasks/07-17-sd-status-command/implement.jsonl: This task metadata file still contains the scaffold `_example` JSONL entry that instructs readers to delete it once real spec/research references are added. Leaving it committed is confusing and looks like... (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path .trellis/tasks/07-17-sd-status-command/check.jsonl: This task metadata file still contains the scaffold `_example` JSONL entry that says to delete it once real entries are added. Please remove the placeholder line (or replace it with real spec/research references). (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path .trellis/tasks/07-17-fleet-candidate-prepare/implement.jsonl: This task metadata file still contains the scaffold `_example` JSONL entry that instructs readers to delete it. Please remove the placeholder line (or replace it with real spec/research references) so task metadata... (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path .trellis/tasks/07-17-fleet-candidate-prepare/check.jsonl: This task metadata file still contains the scaffold `_example` JSONL entry that says to delete it once real entries are added. Please remove the placeholder line (or replace it with real spec/research references). (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #149 review path .github/scripts/run-tests.sh: If `PYTHON_BIN` cannot be resolved (e.g., `command -v` returns empty) or points at a non-executable path, this exports an empty/invalid `SD_AI_COMMAND_PACK_PYTHON`. Because the toolchain resolver only honors this... (https://github.com/platypeeps/sd-ai-command-pack/pull/149)
- **historical** PR #148 review path .github/scripts/generate-command-surfaces.py: The shared-skill reference validator allows nested paths under `references/` (e.g. `references/foo/bar.md`), but `_COMMAND_SOURCE_PATTERNS` only matches a single path segment after `references/`. If a nested... (https://github.com/platypeeps/sd-ai-command-pack/pull/148)
- **historical** PR #148 review path docs/FLEET_ROLLOUT.md: This doc implies disposable tool caches are always used, but `command_environment()` currently uses `env.setdefault(...)`, so an existing `NPM_CONFIG_CACHE` / `UV_CACHE_DIR` / `PYTHONPYCACHEPREFIX` in the caller... (https://github.com/platypeeps/sd-ai-command-pack/pull/148)
- **historical** PR #146 review path tests/test_install_core.py: This assertion depends on a specific newline+indentation sequence ("\n ") in the skill text, making the test brittle against harmless line-wrapping changes. Prefer a regex (or whitespace-normalized text) so the test... (https://github.com/platypeeps/sd-ai-command-pack/pull/146)
- **historical** PR #146 review path tests/test_sdlc_commands.py: The adapter non-exposure test only blocks the `caller=`, `stage=`, and `return-after=` spellings, but the internal orchestration context in the skills is documented using `caller:`, `stage:`, and `return-after:`.... (https://github.com/platypeeps/sd-ai-command-pack/pull/146)
- **historical** PR #145 review path templates/.agents/skills/sd-review-pr/SKILL.md: Inline code span is split across a line break, which renders poorly in Markdown and also prevents the text from matching the earlier “Report exactly: `Finish-work deferred to Stage 4 sd-housekeeping.`” contract.... (https://github.com/platypeeps/sd-ai-command-pack/pull/145)
- **historical** PR #145 review path templates/.agents/skills/sd-review-pr/SKILL.md: The Invocation Mode section says the `defer-finish-work` mode “changes only Step 8”, but Step 1.5 also conditionally changes behavior when `defer-finish-work` is active (it cancels deferral and runs finish-work... (https://github.com/platypeeps/sd-ai-command-pack/pull/145)
- **historical** PR #145 review path templates/.agents/skills/sd-review-pr/SKILL.md: `PR_STATE` is captured once at the start of Step 8 and then reused later to decide whether to run housekeeping. If the PR gets merged while finish-work is running (or while pushing finish-work commits), this stale... (https://github.com/platypeeps/sd-ai-command-pack/pull/145)
- **historical** PR #144 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: In this catch block, using `error.message` directly can throw if a non-Error value is thrown (or if an error-like object lacks `message`), which would mask the original JSON parse failure and potentially crash the... (https://github.com/platypeeps/sd-ai-command-pack/pull/144)
- **historical** PR #144 review path templates/scripts/sd-ai-command-pack-full-check.sh: If `python3` is missing, `sd_ai_command_pack_source_identity` returns status 3, and `warn_unarmed_pack_source_hook` silently returns 0. That means the pack-source hook advisory (about `core.hooksPath`) is skipped... (https://github.com/platypeeps/sd-ai-command-pack/pull/144)
- **historical** PR #143 review path installer/inspection.py: _manifest_platforms() can crash on malformed receipts: if an entry has a non-string (and potentially unhashable) "target" value (e.g., a list), the expression `target in targets` will raise TypeError. This breaks... (https://github.com/platypeeps/sd-ai-command-pack/pull/143)
- **historical** PR #143 review path installer/inspection.py: On audit timeouts, `f"{output}audit timed out"` can concatenate without a separator when the captured output does not end with a newline (e.g. "partial" -> "partialaudit timed out"), reducing diagnostic clarity. Add... (https://github.com/platypeeps/sd-ai-command-pack/pull/143)
- **historical** PR #143 review path .trellis/tasks/archive/2026-07/07-17-installer-status-check-json/implement.jsonl: This archived task artifact still contains the JSONL placeholder/example line. The PR description/checklist says task notes should contain real content (no placeholders). Either replace it with real spec/research... (https://github.com/platypeeps/sd-ai-command-pack/pull/143)
- **historical** PR #143 review path .trellis/tasks/archive/2026-07/07-17-installer-status-check-json/check.jsonl: This archived task artifact still contains the JSONL placeholder/example line. The PR description/checklist says task notes should contain real content (no placeholders). Either replace it with real spec/research... (https://github.com/platypeeps/sd-ai-command-pack/pull/143)
- **historical** PR #142 review path templates/scripts/sd-ai-command-pack-review-preflight.mjs: checkScopeAdvisory currently looks for an exact substring ('the PR body must include') in the scope script output. This is brittle: if the bash script wording changes (even slightly) the preflight will silently stop... (https://github.com/platypeeps/sd-ai-command-pack/pull/142)

### Suggested Preventive Actions
- Move repeated mechanical findings into local checks where possible.
- Keep Copilot instructions focused on current, non-outdated unresolved findings.
- Treat generated or copied payloads as source/sync-contract review surfaces, not style-review surfaces.
<!-- sd-review-learnings:end -->
