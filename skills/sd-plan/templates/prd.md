---
title: <one line, what this delivers>
created: <YYYY-MM-DD>
---

<!-- Template instruction: read .status-source in the selected work root
(docs/work by default, or the root chosen with --work-dir) before writing this
item. With `row`, keep status in the database and add no status field here.
With `file` or no marker, preserve the legacy reader by adding `status: planning` to
frontmatter. Report an unrecognized marker instead of choosing a source.
Remove this instruction from the finished PRD. -->

# PRD — <slug>

## Problem

<What is wrong today, in terms someone outside this work would recognise. If the
problem is only visible as a feeling, say what you measured to make it concrete.>

## Requirements

1. <Each requirement testable by someone who did not write it.>

## Acceptance criteria

- [ ] <A check with a result, not an intention. "`pytest tests/auth` passes with
      0 failures", not "tests pass".>

## References

<Links, prior work, the issue this came from. Delete the heading if empty.>

## Log

- <YYYY-MM-DD> created
