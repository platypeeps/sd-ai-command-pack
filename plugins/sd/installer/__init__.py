"""Implementation package for the sd-ai-command-pack installer.

install.py stays the executable entry point; these modules hold the
implementation in one-way dependency order (see the task design.md).
"""
