{"file": ".trellis/spec/tooling/vendored-trellis-compatibility.md", "reason": "The acceptance criterion for base_branch requires the check to behave identically on a task_store.py with and without resolve_default_branch. This contract defines that dual-version obligation and is the standard the check is measured against."}
{"file": ".trellis/spec/guides/cross-layer-thinking-guide.md", "reason": "The four defects all live at layer boundaries — SKILL.md prose, vendored task.py, the review preflight, and task.py archive each hold a partial view of what a correctly seeded task is, and none sees the whole. Use its data-flow mapping to confirm the stage check closes those boundaries rather than adding one more partial view."}
