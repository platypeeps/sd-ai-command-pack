{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Section at :1108-1182 is the governing contract for changes to `final-bundle --mode completion` in review-preflight.mjs, its template twin, and eligibility revalidation. :1122-1127 fixes the post-archive-review-successor result shape, which this change must leave untouched; :1149-1152 requires eligibility to rerun the same validator and match the complete JSON document, which is why the gate stays a committed-content comparison. The completion-bundle archive-identity rule this task changes is recorded in the Review Preflight Runtime Contract at :527-540."}
{"file": ".trellis/spec/frontend/adapter-guidelines.md", "reason": "The wrapper-preserving lifecycle chaining scenario owns what sd-finish-work adds around the delegated Trellis lifecycle, including the branch preparation that must precede the finalization base capture."}
{"file": ".trellis/spec/backend/manifest-and-filesystem.md", "reason": ":41 defines templates/** as shipped payload and :855 requires templates authoritative with root mirrors regenerated by `make sync`. Both files this task edits are mirrored pairs, so the change also carries a manifest bump and a refreshed fleet candidate ledger; :72-74 permits exactly one provenance.candidate-stale finding as the trigger for that refresh."}
