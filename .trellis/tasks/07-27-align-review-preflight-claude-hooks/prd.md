# Align review-preflight classifier for Claude hooks

## Goal

Keep the JavaScript preflight copied-path classifier aligned with review scope for .claude/hooks/.

## Requirements

- Add `.claude/hooks/` to the JavaScript preflight classifier's Trellis/runtime copied-path set.
- Keep the root script and shipped template byte-aligned.
- Add focused parity coverage so the shell and JavaScript classifiers cannot silently diverge for platform hook paths.

## Acceptance Criteria

- [x] `.claude/hooks/*` is classified as copied/generated by both review-scope
  implementations. Added `path.startsWith('.claude/hooks/')` to
  `isTrellisCopiedPath` in the mjs (root + template twin, byte-identical); the
  shell `is_trellis_runtime_path` already listed `.claude/hooks/*`.
- [x] Focused classifier tests and `make check` pass. New `copiedTemplateKind`
  assertion in tests/test_review_preflight.py and behavioral shell-advisory
  test `test_claude_hooks_are_scoped_as_copied` in tests/test_review_scope.py;
  both affected modules green (114 tests) and `make check` exits 0.
- [x] The next release payload and consumer audit remain valid. Version bumped
  0.64.9 → 0.64.10, candidate ledger regenerated, and `make check` install/audit
  gates pass at the new digest.

## Notes

- Source finding: https://github.com/platypeeps/sd-github-review/pull/28#discussion_r3659271061
- This is a lightweight PRD-only hardening task deferred by the fleet finding-severity gate.
