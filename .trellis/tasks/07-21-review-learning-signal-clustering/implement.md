# Implementation plan: recurring review-learning signal clustering

1. Define expected clustered Markdown with synthetic duplicate and current
   actionable fixtures.
2. Add pure signature, category, aggregation, ranking, and bounding helpers to
   the template review-learnings script, then synchronize the root copy.
3. Update rendering so current actionable comments remain individual and
   historical evidence becomes counted clusters with bounded examples.
4. Generate preventive suggestions only for detected recurring categories.
5. Preserve CLI selection, pagination, dry-run, update idempotence, target-file
   replacement, and Markdown sanitization through regression tests.
6. Update the shared skill/docs contract and regenerate/synchronize platform
   surfaces as required.
7. Run focused review-learning tests, template parity, install audit, and the
   repository full-check.

Do not start this task until the user reviews the planning artifacts.
